{
  "name": "cue",
  "version": "1.0.0",
  "description": "Experimental generative art using sentiment analysis",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev:frontend\" \"npm:dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uv run uvicorn server:app --host 0.0.0.0 --port 8001 --reload",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && uv sync",
    "serve": "ENV_SERVICE_FILE=./ops/systemd/.env.systemd ./ops/systemd/run.sh",
    "status": "systemctl status cue-backend",
    "logs": "journalctl -u cue-backend -f",
    "restart": "systemctl restart cue-backend && systemctl reload nginx"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "workspaces": [
    "frontend"
  ]
} 